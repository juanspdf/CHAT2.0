# Variables de entorno para Docker Compose
# Copia este archivo a .env y ajusta los valores

# JWT Secret (CAMBIAR EN PRODUCCIÓN)
JWT_SECRET=super-secret-jwt-key-change-in-production-to-something-secure

# MongoDB (si usas MongoDB Atlas en lugar del container)
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/chat-system

# Redis Password (opcional, si quieres proteger Redis)
# REDIS_PASSWORD=tu-password-seguro

# Puertos (si quieres cambiar los puertos por defecto)
# BACKEND_PORT=5000
# FRONTEND_PORT=5173
# MONGODB_PORT=27017
# REDIS_PORT=6379
